<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Buscar CEP</title>
  </head>
  <body>
    <% if (message) {%>
    <div style="color: red;">
      <%= message %>
    </div>
    <%}%>
    <form action="/cep" method="POST">
      <label for="cep">Digite o CEP</label>
      <input type="text" name="cep" id="cep" />
      <button type="submit">Pesquisar</button>
    </form>
    <% if (cepData) {%>
    <ul>
      <li>CEP: <%= cepData.cep %></li>
      <li>UF: <%= cepData.uf %></li>
      <li>Cidade: <%= cepData.cidade %></li>
      <li>Bairro: <%= cepData.bairro %></li>
      <li>Logradouro: <%= cepData.logradouro %></li>
    </ul>
    <% } %>
  </body>
</html>
